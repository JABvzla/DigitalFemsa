{
  "name": "DigitalFemsa",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "mobile:install": "npm install --prefix ./infrastructure/mobile",
    "mobile:android": "npm run --prefix ./infrastructure/mobile android",
    "mobile:ios": "npm run --prefix ./infrastructure/mobile ios",
    "mobile:start": "npm run --prefix ./infrastructure/mobile start",
    "test": "npm run --prefix ./infrastructure/mobile test",
    "test:watch": "npm run --prefix ./infrastructure/mobile test:watch",
    "mobile:lint": "npm run --prefix ./infrastructure/mobile lint"
  },
  "devDependencies": {
    "@babel/runtime": "^7.21.0",
    "@types/jest": "^26.0.23",
    "jest-fetch-mock": "^3.0.3",
    "typescript": "^4.8.3"
  }
}
